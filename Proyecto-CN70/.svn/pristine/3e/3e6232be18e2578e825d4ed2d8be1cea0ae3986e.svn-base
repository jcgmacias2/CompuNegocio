using Aprovi.Data.Core;

namespace Aprovi.Business.Services
{
    public class AproviFacturaService : FacturaService
    {
        public AproviFacturaService(IUnitOfWork unitOfWork, IConfiguracionService config, IComprobantFiscaleService fiscalReceipts, ICotizacionService quotes, IClienteService clients, IArticuloService items, INotaDeCreditoService creditNote, ISerieService series, ICatalogosEstaticosService staticCatalogs) : base(unitOfWork, config, fiscalReceipts, quotes, clients, items, creditNote, series, staticCatalogs)
        {
        }
    }
}
